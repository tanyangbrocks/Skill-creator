namespace SkillCreator.World;

public static class TileWorldConstants
{
    // Forwards to WorldScale — change WorldScale.Grain to adjust granularity.
    public static float TileSize => WorldScale.TileSize;
}
